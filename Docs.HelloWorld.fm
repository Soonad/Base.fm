// This imports our base libraries.
import Base@0

// This is the “Hello, World!”:
hello_world : Output
  print("Hello, world!")

// You can run it by clicking on `hello_world`. This also checks its type. On
// the terminal, you can write `fm file/hello_world` to run it on the debug
// mode, or use the `-o` flag can be used to run it optimally, or the `-t` flag
// to type-check it. For more options, write just `fm`.
